import Renderer from "./client";

export const metadata = {
  title: "DaisyUI Puck Demo",
  description: "Demo of a page with DaisyUI components built using Puck.",
};

export default function Page() {
  return <Renderer />;
}
